import 'package:anmation/home.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(Animated_types());
}

class Animated_types extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Homepage(),
    );
  }
}
